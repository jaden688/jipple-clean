Legacy compatibility mirror only.

Canonical runtime agent data now lives in:

- `jl_engine_core/data/agents/`

Do not edit files in `jl_engine_core/data/personas/` directly unless you are doing a deliberate compatibility sync for older external tooling.
